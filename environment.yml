name: gdec

channels:
  - pytorch
  - conda-forge
  - gpytorch

dependencies:
  - conda-build
  - pip

  # Package dependencies
  - numpy
  - pytorch
  - gpytorch
  - scipy
  - scikit-learn

  # Executable dependencies
  - ipykernel
  - matplotlib
  - pandas

  # Development dependencies
  - black
  - flake8
  - flake8-blind-except
  - flake8-builtins
  - flake8-docstrings
  - flake8-mutable
  - hypothesis
  - isort
  - mypy
  - pep8-naming
  - pytest

  - pip:
    - jax
    - jaxlib
    - flake8-bugbear
